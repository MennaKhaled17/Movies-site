<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movie Details</title>
    <style>
        .movie {
            margin-bottom: 20px;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <h1><strong>Movie Details</strong></h1>
  <div class="moviesss">
    <% if (movie) { %>
      <h2 class="c1"><%= movie.title %></h2>
      <div class="p1">
        <p><strong>Description:</strong> <%= movie.description || 'No description available.' %></p>
        <p><strong>Genre:</strong> <%= movie.genre || 'Genre not specified' %></p>
        <p><strong>Price:</strong> <%= movie.price || 'N/A' %></p>
        <p><strong>Timing:</strong> <%= movie.timing || 'Not available' %></p>
      </div>
    <% } else { %>
      <p><%= console.log(movie); %></p>
    <% } %>
  </div>
</body>
</html>
